Rails.application.config.active_job.queue_adapter = :solid_queue

SolidQueue.configure do |config|
  config.polling_interval = 1.second
  config.concurrency = ENV.fetch("RAILS_MAX_THREADS", 5).to_i
end